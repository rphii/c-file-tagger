#include "tag.h"

void tag_free(Tag *tag) { //{{{
    //trstr_free(&tag->tags);
    //str_free(&tag->filename);
} //}}}

void tagref_free(TagRef *tagref) { //{{{
    //trstr_free(&tagref->filenames);
    //str_free(&tagref->tag);
} //}}}

